# servicenow-instance-to-instance-integration
This project simulates an Instance-to-Instance (I2I) integration between two ServiceNow Personal Developer Instances (PDIs).  The objective was to create automated incident synchronization using REST APIs, asynchronous Business Rules and custom relationship fields.
